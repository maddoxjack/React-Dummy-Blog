import styled from "styled-components";

const Wrapper = styled.div`
  min-height: calc(100vh - 64px);
  margin: 0 auto;
  max-width: 60rem;
`;

//height calc is done to give pixels to header and footer at roughly 64 px

export default Wrapper;
